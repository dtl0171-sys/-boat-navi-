name: boat_navi
description: プレジャーボート向けナビゲーションアプリ
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  latlong2: ^0.9.0
  geolocator: ^13.0.2
  http: ^1.1.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  pointer_interceptor: ^0.10.1
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
